# React Frontend Service Environment Configuration Template

# Use '/api' when running in Docker Compose (proxied via Nginx)
# Use 'http://localhost:5001/api' when running standalone with 'npm run dev'
VITE_API_URL=/api
